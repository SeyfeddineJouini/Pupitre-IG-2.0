/* Ce component correspond au component OFF après une inactivité longue. */
import React from 'react';
import "./Lock_ExtremePage.css"

const Lock_ExtremePage= () => {
    return (
        <div className = "off-screen" > 
        </div>
    )
}

export default Lock_ExtremePage;